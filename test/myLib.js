const add = (a, b)=> a + b 
const multiple = (a, b)=> a * b 
const subtract = (a, b)=> a - b 
const divide = (a, b)=> a / b 
const power2 = (a)=> a ** 2 

module.exports = { add, multiple, subtract, divide, power2 }